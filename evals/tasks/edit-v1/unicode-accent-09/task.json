{
  "schema": 1,
  "id": "unicode-accent-09",
  "budget": { "timeout_ms": 300000, "provider_requests": 20 },
  "phases": [{ "action": "prompt", "prompt": "In menu.txt, change only `cafe` to `café` and `creme brulee` to `crème brûlée`. Preserve the existing French text.", "permission": "allow" }],
  "success": { "files": { "menu.txt": "café\ncrème brûlée\npiñata\n" } }
}
