{
  "schema": 1,
  "id": "newline-bom-15",
  "budget": { "timeout_ms": 300000, "provider_requests": 20 },
  "phases": [{ "action": "prompt", "prompt": "In bom.txt, change enabled=false to enabled=true. Preserve the UTF-8 BOM, LF line endings, and final newline.", "permission": "allow" }],
  "success": { "files": { "bom.txt": "﻿enabled=true\nname=sample\n" } }
}
