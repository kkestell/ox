{
  "schema": 1,
  "id": "newline-lf-13",
  "budget": { "timeout_ms": 300000, "provider_requests": 20 },
  "phases": [{ "action": "prompt", "prompt": "In unix.conf, set retries to 5. Preserve its LF line endings and final newline.", "permission": "allow" }],
  "success": { "files": { "unix.conf": "timeout=30\nretries=5\nmode=safe\n" } }
}
