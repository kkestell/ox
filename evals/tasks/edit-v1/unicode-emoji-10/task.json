{
  "schema": 1,
  "id": "unicode-emoji-10",
  "budget": { "timeout_ms": 300000, "provider_requests": 20 },
  "phases": [{ "action": "prompt", "prompt": "In status.txt, change the rocket status from `launching` to `launched` and replace the hourglass with a check mark. Preserve the emoji and spacing on all other lines.", "permission": "allow" }],
  "success": { "files": { "status.txt": "🚀 launched\n✅ complete\n🛰️ telemetry nominal\n" } }
}
